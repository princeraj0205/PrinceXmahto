# Portfolio V2

The redesign is kept in `portfolio-v2/` as a modular reference while the root site remains deployment-safe.

## Live project
Number Lookup is deployed separately and linked from the portfolio project page.

## Editing
Add new projects as dedicated pages and link them from `projects.html`. Keep API credentials server-side and out of public HTML/JavaScript.
